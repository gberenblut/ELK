# ELK
ELK stack deployment

this repo contains a single yml file to be run with ansible.
it also contains two configuration files. one for filebeat and one for logstash.
the playbook creates only a two node elasticsearch cluster as anymore would crash my computer due to a lack of memory.
